# F5-TTS Docker 部署环境变量配置

# ===========================================
# 管理员认证配置
# ===========================================
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password_here

# 是否启用认证 (true/false)
ENABLE_AUTH=true

# ===========================================
# Cloudflare Tunnel 配置
# ===========================================
# 方式1: 使用 Tunnel Token (推荐)
# 从 Cloudflare Zero Trust Dashboard 获取
# https://one.dash.cloudflare.com/
CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here

# 方式2: 使用 Tunnel ID 和 Credentials (备选)
# CLOUDFLARE_TUNNEL_ID=your_tunnel_id
# CLOUDFLARE_CREDENTIALS_FILE=/path/to/credentials.json

# ===========================================
# Cloudflare 域名配置
# ===========================================
# 你的域名，例如: f5-tts.yourdomain.com
CLOUDFLARE_DOMAIN=f5-tts.yourdomain.com

# ===========================================
# F5-TTS 配置
# ===========================================
# Gradio 服务端口 (容器内)
GRADIO_SERVER_PORT=7860

# 模型缓存路径 (容器外挂载)
# MODEL_CACHE_PATH=/path/to/cache

# ===========================================
# GPU 配置
# ===========================================
# NVIDIA GPU 配置
NVIDIA_VISIBLE_DEVICES=all

# ===========================================
# 网络配置
# ===========================================
# Nginx 监听端口 (宿主机)
NGINX_PORT=8080

# F5-TTS 直接访问端口 (宿主机，可选)
# F5TTS_PORT=7860
