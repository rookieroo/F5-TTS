# 请使用以下命令生成密码文件:
# docker run --rm httpd:alpine htpasswd -nbB admin your_password > .htpasswd
# 
# 或者使用 Python:
# python3 -c "import bcrypt; print('admin:' + bcrypt.hashpw(b'your_password', bcrypt.gensalt()).decode())"
#
# 默认用户名: admin
# 默认密码: changeme (请务必修改!)

# 示例格式 (使用 bcrypt):
# admin:$2y$05$encrypted_password_hash
